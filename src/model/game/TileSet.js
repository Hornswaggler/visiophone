// import { makeEntity } from './Entity';
// import {ENTITY_TYPE} from './EntityConfig';

export const 

export default () => {
  return new (() => ({

    getTileForMapPosition({mapData, x, y}){

    }
  }));
}
